<h1>Search Page</h1>

<%= form_tag("/search", :method => "get") do %>
  <%= label_tag(:search, "Search for:") %>
  <%= text_field_tag(:search) %>
  <%= submit_tag("Search") %>
<% end %>

<% @users.each do |user| %>
  <div><%= user.username %></div>
<% end %>