<h1><%= @question.question_text %></h1>

<ul>
  <% @question.answers.each do |answer| %>
    <li><%= answer.answer_text %>
      <% if answer.is_correct == "on" %> ✔<% end %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>

