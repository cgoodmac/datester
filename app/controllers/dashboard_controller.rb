class DashboardController < ApplicationController
    def index
      # if @auth.photos.where(:is_primary => true).first.image.thumb.url == true
      #   @avatar = @auth.photos.where(:is_primary => true).first.image.thumb.url
      # else
      #   @avatar = nil
      # end
    end
end